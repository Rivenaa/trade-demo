# 面试讲解稿 —— ProtoCircuit 外贸 B2B 网站

> 用途：HR 或面试官问到这个项目时，按这个稿子讲。**如实说明这是个人作品**，个人作品 + 讲得清楚业务，比来路不明的"公司项目"有说服力得多。

## 一句话介绍

"这是一个我独立开发的外贸 B2B 网站 Demo，模拟深圳 PCB 工厂面向海外买家的官网商城：英文为主、中英双语切换，包含产品目录、阶梯价、询盘和 RFQ 批量询价的完整流程，前端 Next.js + TypeScript + Tailwind，询盘后端用的 Next.js API Route，部署在 Vercel。"

## 为什么做这个（动机，30 秒）

"我关注到外贸 B2B 网站和 B2C 商城的差别：买家是公司采购，决策需要 **MOQ、阶梯价、交期、规格参数、工厂资质** 这些信息，转化动作不是'立即购买'而是'发询盘'。所以我按这个业务模型做了这个 Demo，想证明自己理解这类业务，而不只是会写页面。"

## 功能 ↔ 业务概念对照（被问"你懂 B2B 吗"时用）

- **产品目录 + 类目筛选 + 搜索** → SKU/类目体系
- **阶梯价表格（≥5 / ≥50 / ≥200 量价递减）+ MOQ + FOB 条款** → B2B 定价与贸易条款
- **询盘表单（产品自动预填、字段校验、询盘编号）** → Inquiry 系统，B2B 站的核心转化点
- **询盘篮（多产品加篮、Header 角标、合并成一条询价提交）** → B2B 标志性交互：买家一次采购多个 SKU，"合并询盘"而非"立即下单"（阿里国际站同款）
- **RFQ 页（一次询多个产品、动态增删条目）** → RFQ（Request for Quotation）流程
- **中英切换** → 外贸站多语言（海外买家英文，国内同事中文）
- **规格表（板材、铜厚、表面处理…）** → 工业品买家决策依据

## 技术亮点怎么讲

1. **App Router 的服务端/客户端组件分工**：产品详情页 `page.tsx` 是服务端组件，负责 `await params` 取数据；交互部分（语言切换、表单）拆成客户端组件。好处：首屏 HTML 直出，SEO 友好。
2. **SSG**：`generateStaticParams` 在构建时把 16 个产品页全部静态化，访问零等待；`generateMetadata` 按产品生成页面标题。
3. **多语言方案**：自研轻量方案——`en.ts` / `zh.ts` 两个字典对象 + React Context，TypeScript 接口强制两种语言字段同步（少翻译一个 key 直接编译报错），选择存 localStorage。
4. **前后端错误码约定**：API 返回 `{ errors: { email: "emailInvalid" } }` 这样的错误码，前端按当前语言映射成文案——一份接口，双语提示。
5. **表单工程**：HTML5 语义 + 受控/非受控混用、提交中防重复点击、成功态展示询盘编号。
6. **询盘篮状态设计**：独立 `BasketProvider`（Context）管理条目，localStorage 持久化；加篮时数量默认取产品 MOQ；用 `mounted` 标志解决 SSR 首帧与 localStorage 恢复状态的水合不一致；提交时复用 `/api/inquiry` 的 RFQ 分支，后端零改动。
7. **Tailwind v4 主题令牌**：`@theme` 定义品牌色，全站统一视觉。

## 预设追问 & 参考回答

**Q：询盘数据存到哪里了？**
A：Demo 里校验通过后生成询盘编号、打印日志，没接数据库——这是个刻意的边界。生产方案我会这么做：写入数据库（PostgreSQL/MySQL）→ 触发邮件或 WhatsApp 通知销售 → 对接 CRM 做询盘分配；另外要加图形验证码和 IP 限流，外贸站的询盘表单是垃圾广告重灾区。这个链路我能讲清，也有信心搭出来。

**Q：多语言为什么不用路由（/en、/zh）？**
A：Demo 规模下 Context 方案最简单。真实外贸站为了 SEO 会做成路由级多语言（`[lang]` 动态段）+ `hreflang` 标签，让 Google 按语言收录——我知道这个方案，项目里留了注释说明。

**Q：产品数据为什么放本地文件？**
A：模拟后端返回的目录数据，重点是数据建模：类型化的 Product/Category/PriceTier 结构。接真实后端时只需把 `getProduct()` 改成查数据库或调 CMS 接口，页面代码不用动。

**Q：如果继续迭代，你下一步做什么？**
A：①产品图片接入真实图库 + `next/image` 优化；②询盘落地数据库 + 邮件通知；③多语言改路由级方案做 SEO；④加 GA/埋点分析询盘转化漏斗——外贸站最终是按询盘量考核的。

**Q：遇到了什么难点？**
A：Next.js 里 `useSearchParams` 必须包 Suspense 边界，否则构建报错——我查了文档理解了原因：预渲染时查询参数还不存在，需要声明客户端渲染边界。另外 Next 16 的 `params` 是 Promise，详情页要 `await` 才能取 slug。

## 简历写法（个人项目栏，如实标注）

```
外贸 B2B 网站 Demo（个人项目）  Next.js / TypeScript / Tailwind CSS
· 独立完成 PCB 外贸工厂站：产品目录、阶梯价、询盘、RFQ 批量询价全流程
· App Router 服务端/客户端组件分离，generateStaticParams 静态生成 16 个产品页
· 自研轻量中英双语方案（Context + 类型化字典），API 错误码驱动双语表单提示
· 演示链接：https://xxxx.vercel.app    源码：https://github.com/xxxx
```
